<!-- <script>
  let canvas;

  // const createCanvas = () => {
  //   let ctx = canvas.getContext("2d");
  //   canvas.width = $imgElement.width;
  //   canvas.height = $imgElement.height;

  //   ctx.drawImage($imgElement, 0, 0, canvas.width, canvas.height);

  //   phase.update((item) => (item = 3));

  // tauri.invoke("edit_image", {
  //   image: currentImage,
  //   path: await path.downloadDir(),
  // });
  // };
</script>

<div
  class="space-y-10 w-full min-h-screen flex flex-col justify-center items-center">
  <img class="border shadow-lg" bind:this={$imgElement} src="" alt="" />

  <button
    class="bg-blue-600 text-white px-4 py-2 rounded mx-auto"
    on:click={createCanvas}>Start Edit</button>
</div>

<div class="border border-black">
  <canvas bind:this={canvas} />
</div> -->
